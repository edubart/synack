ABOUT
  Tool used for flooding TCP services with the 3-way TCP handshake causing
  massive amount of connections on the host, exhausting it's resources and
  leading to DOS(Denial of Service). Created for educational and testing
  purposes.

AUTHORS
  edubart   - https://github.com/edubart

INSTALL
  Installation steps:
    $ git clone git://github.com/edubart/synack.git
    $ make
    $ sudo make install

USAGE
  Your might want to add the following iptables rule to prevent the kernel saboting connection attacks:
    $ iptables -I OUTPUT -p tcp --tcp-flags ALL RST -j DROP
  For using it see the --help.

